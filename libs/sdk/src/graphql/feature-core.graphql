fragment AppConfigDetails on AppConfig {
  authLinkProviders
  authLoginProviders
  authPasswordEnabled
  authRegisterEnabled
}

fragment PagingMetaDetails on PagingMeta {
  currentPage
  isFirstPage
  isLastPage
  nextPage
  pageCount
  previousPage
  totalCount
}

query uptime {
  uptime
}

query appConfig {
  config: appConfig {
    ...AppConfigDetails
  }
}

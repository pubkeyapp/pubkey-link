# https://github.com/docker/awesome-compose/blob/master/prometheus-grafana/grafana/datasource.yml
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090
    isDefault: true
    access: proxy
    editable: true
